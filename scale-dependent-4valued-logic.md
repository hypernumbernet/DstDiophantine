# スケール依存の四値論理

スケールが大きくなると理想的な四値論理が現れ、小さくなると二値論理に収縮する——
この文書では、そのアイデアを入門から数理的定式化まで説明し、続けて Lean で機械検証した結果を整理します。

**検証の現状。** 振幅、四状態、結合子の非爆発、二つの順序、Killing 重なり、異軸干渉、大規模ポテンシャルの臨界点は証明済みです。一方、書かれたポテンシャルからは「小スケールで四値から二値へ収縮する」は従いません。詳細は [§13](#13-lean-検証で分かったこと) にあります。このプロジェクトの論理は D4L です。入口は `DstDiophantine/Logic.lean`（`Basic.lean` には入れません）。

---

## 1. そもそも何の話か？

普通、論理は「真か偽か」の二値で考えることが多いです。
しかし、もっと豊かな論理（四値論理）も存在します。

- **二値論理**：真（True）と偽（False）だけ
- **四値論理**：真・偽に加えて、「未決定」と「真でも偽でもある」という状態を認める

ここで提案しているのは、次のような**スケールによって性格が変わる論理**です。

| スケール | 論理の様子 |
|----------|------------|
| とても大きい（無限大に近い） | 理想的な四値論理が安定して現れる |
| 小さい | 中間の状態が消えて、実質的に二値論理に収縮する |

つまり、論理そのものが「大きさ」によって相転移のように姿を変える、という発想です。

---

## 2. 身近な例えで理解する

### 例え1：温度と水

水は温度によって姿を変えます。

- 高い温度 → 水蒸気（広がった状態）
- 低い温度 → 氷（固まって結晶になる）

これと同じように、論理も「スケール」という一種の温度のようなパラメータで姿を変える、と考えます。

### 例え2：量子アニーリング

量子コンピュータの一種である量子アニーリングでは、「真の量子効果」を理想としつつ、現実的には「疑似量子」的な手法で近似します。

- 理想（大きなスケール・強い量子性） → 豊かな重ね合わせが可能
- 現実的な有限のスケール → より古典的な振る舞いに近づく

この文書の論理も、同じような「理想と現実の間」を滑らかにつなぐものです。

### 例え3：大きなLLMと小さなモデル

大規模言語モデル（LLM）でも似た現象が見られます。

- 非常に大きなモデル → 曖昧さや矛盾をある程度保持した、豊かな内部表現が可能
- 小さなモデル → より白黒はっきりした、二値的な判断に傾きやすい

スケールが表現の豊かさを支えている、という点で共通しています。

---

## 3. 四値論理の四つの状態

このアイデアのベースになっている四値論理では、次の四つの状態を考えます。

| 状態 | 意味 | イメージ |
|------|------|----------|
| **T** (True) | 真 | 完全に一致している状態 |
| **U** (Undetermined) | 未決定 | まだどちらとも言えない中間状態 |
| **F** (False) | 偽 | 完全に食い違っている状態 |
| **B** (Both) | 真でも偽でもある | 矛盾を含みながらも安定している状態 |

特に重要なのは **B** です。
普通の二値論理では「真であり偽である」は爆発（何でも導けてしまう）を起こしますが、この論理では **B** を「安定した固定点」として認めます。

これらの状態は、後述する**正規化**トーションスカラー \(J_{\mathrm{norm}} \in [-1,1]\) の値によって識別されます。生の \(J\) の許容天井は \(3\pi^2/8\) であり、区間 \([-1,1]\) は \(J_{\mathrm{norm}}\) のものです。

---

## 4. なぜスケールで変わるのか？（直感的説明）

核心は「安定性」にあります。

- スケールが大きいとき
  → 中間状態（U）や矛盾を含む状態（B）が、エネルギー的に安定して存在できる、というイメージ。

- スケールが小さいとき
  → これらの中間状態が不安定になり、残るのは T と F だけになる、というイメージ。

数学的には、真理値の動きを決める「ポテンシャル（エネルギーの谷）」が、スケールによって形を変える、と表現できます。
ただし **§6 に書いた \(U\) のままでは、この小スケール収縮は起きません**（[§13.2](#132-書かれた-u-では-b-井戸は消えない)）。相転移は、まだ証明されていない別の \(U\) を要する仮説です。

次の節から、このイメージをDST（二重時空理論）の言葉と数式で正確に定式化します。

---

## 5. トーションスカラー \(J\) と四状態の対応

DSTにおいて、通常時空ローター \(R_{\mathrm{usual}}\) と双対時空ローター \(R_{\mathrm{dual}}\) の相対関係は

\[
\Omega = R_{\mathrm{usual}}^{\dagger} R_{\mathrm{dual}}, \qquad
\Omega_{\mathrm{biv}} = \log \Omega
\]

で与えられます。パラメータ展開 \(\Omega=\sum(\alpha_a/2)B^{+}_a+(\beta_a/2)B^{-}_a\) のもと、プロジェクトが使う生のトーションは

\[
J = \frac12\sum_{a=1}^{3}\bigl(\alpha_a^2-\beta_a^2\bigr)
\]

です。許容錐（各軸 \(\alpha_a\ge 0\)、\(\beta_a\ge 0\)、\(\alpha_a+\beta_a\le\pi/2\)）の上で \(|J|\le 3\pi^2/8\) です。古い草稿の \(\frac1{16}B(\Omega_{\mathrm{biv}},\Omega_{\mathrm{biv}})\) は、同じ展開では \(J\) と定数倍だけずれます（Lean: `paper_appendix_killing_coeff_false`）。

真理値の座標は、天井を \(1\) に直した正規化

\[
J_{\mathrm{norm}} := \frac{8}{3\pi^2}\,J \in [-1,1]
\]

です。許容錐上の有界 \(|J_{\mathrm{norm}}|\le 1\)、像がちょうど \([-1,1]\)、等号 \(|J_{\mathrm{norm}}|=1\) が全軸純双曲または全軸純楕円であること、はすでに証明済みです。

この \(J_{\mathrm{norm}}\) を真理値とみなし、次のように四状態を対応させます：

\[
\begin{align*}
\lvert T\rangle &\colon && J_{\mathrm{norm}} = 0 && \text{（完全同期）}\\
\lvert U\rangle &\colon && 0 < J_{\mathrm{norm}} < 1 && \text{（boost優勢・未決定）}\\
\lvert F\rangle &\colon && J_{\mathrm{norm}} = +1 && \text{（最大正トーション）}\\
\lvert B\rangle &\colon && -1 \le J_{\mathrm{norm}} < 0 && \text{（rotation優勢・パラコンシステント領域）}
\end{align*}
\]

特に \(\lvert B\rangle\) は負トーション領域です。結合子 \(j\wedge\neg j=-|j|\) は決して \(\lvert F\rangle\) にならず、爆発しません。ただし \(\lvert B\rangle\) の**ラベル全体**が一つの安定固定点なのではなく、大規模ポテンシャルの安定点は端点 \(J_{\mathrm{norm}}=-1\)（最深の \(B\)）です。

双対時空だけを考えると、比較的対称的で量子的・多値的な構造が現れやすくなります。
そこに通常時空との差である \(J\) が加わることで、方向性やバイアスが生まれます。

---

## 6. スケール依存ポテンシャルの定式化

スケールパラメータ \(\lambda > 0\)（コヒーレンス長・系の大きさ・有効温度の逆数などに対応）を導入し、有効ポテンシャルを

\[
V_{\lambda}(J) = -\cos(2\pi J) + \frac{\alpha}{\lambda}\, U(J)
\]

と定めます。ここで \(\alpha > 0\) は結合定数、\(U(J)\) は正の領域を相対的に安定化させる項です。典型的な選択は

\[
U(J) =
\begin{cases}
0 & (J < 0) \\
J^{2} & (J \ge 0)
\end{cases}
\]

またはより滑らかな

\[
U(J) = \frac{1}{2}\bigl(J + |J|\bigr)^{2}.
\]

この二つは同じ関数ではありません。正領域で前者は \(J^2\)、後者は \(2J^2\) です（Lean: `USmooth_eq_two_UPiece_of_pos`）。どちらを採用するかは、以降の定量評価に効きます。

力学は勾配流

\[
\frac{dJ}{dt} = -\frac{\partial V_{\lambda}}{\partial J}
\]

で与えられます。

### 大規模極限と小規模極限

- **大規模極限** \(\lambda \to \infty\)
  \(\dfrac{\alpha}{\lambda}U(J)\) が消失し、

  \[
  V_{\infty}(J) = -\cos(2\pi J)
  \]

  となります。臨界点は \(J = k/2\)（\(k \in \mathbb{Z}\)）です。区間 \([-1,1]\) では安定点が \(\{-1,0,+1\}\)、鞍点が \(\{\pm 1/2\}\) です。

  勾配流 \(\dot J=-V_\infty'(J)\) の盆地は次のとおりです。

  | 区間 | 流れ先 |
  |------|--------|
  | \((-1,-1/2)\) | \(-1\)（最深 \(\lvert B\rangle\)） |
  | \((-1/2,1/2)\) | \(0\)（\(\lvert T\rangle\)） |
  | \((1/2,1)\) | \(+1\)（\(\lvert F\rangle\)） |

  したがって負側のアトラクタ盆地は \([-1,-1/2)\) であり、\([-1,0)\) 全体ではありません。開区間 \((0,1)\) は \(\lvert U\rangle\) のラベルですが、内部の安定点ではなく、\(0\) または \(+1\) へ分かれます。

- **小規模極限** \(\lambda \to 0^{+}\)（書かれた \(U\) での実際）
  \(J<0\) で \(U=0\) なので、負半直線上の \(V_\lambda\) は \(\lambda\) に依らず \(V_\infty\) のままです。\(J=-1\) の局所安定性は消えません。正領域の \(J^2\) 項は \(J=+1\) を不安定化もしません。**書かれた \(U\) からは二値への収縮は従いません。**

「スケールで論理の姿が変わる」は、別の \(U\)（負領域で凹、または周期項の前に \(\lambda\) が乗る形）を置いて初めて検証対象になる主張です。

---

## 7. 固定点の安定性

ポテンシャルの二階微分により線形安定性を判定できます。

\[
V_{\lambda}''(J) = (2\pi)^{2}\cos(2\pi J) + \frac{\alpha}{\lambda} U''(J)
\]

- 大規模極限では \(V_\infty''(-1)=V_\infty''(0)=V_\infty''(1)=4\pi^2>0\)（局所安定）、\(V_\infty''(\pm 1/2)=-4\pi^2<0\)（不安定）。
- 書かれた区分的 \(U\) では \(J<0\) で \(U''=0\) なので、すべての \(\lambda>0\) で \(V_\lambda''(-1)=4\pi^2>0\) です。**臨界スケール \(\lambda_c\) は存在しません**（Lean: `written_U_no_critical_scale`）。

\(\lambda_c\) を「負トーション盆地が消滅する点」と定義するなら、まずその消滅が起きる \(U\) を選ぶ必要があります。

---

## 8. 論理結合子のスケール変形

結合子自体も \(\lambda\) 依存に拡張できます。例えば合接をソフトミンで変形します：

\[
a \land_{\lambda} b = -\frac{1}{\beta(\lambda)}\log\Bigl(e^{-\beta(\lambda)a} + e^{-\beta(\lambda)b}\Bigr)
\]

ここで \(\beta(\lambda) = \beta_{0}\,\lambda\) と取ります。すると

- \(\lambda \to \infty\)（\(\beta\to\infty\)）で通常の \(\min(a,b)\) に収束する（証明済み。Lean: `tendsto_softmin_atTop`）。
- \(\lambda \to 0\)（\(\beta\to 0^{+}\)）では softmin は \(\min\) より真に小さく、有限の \(\beta>0\) では決して \(\min\) に一致しません（Lean: `softmin_lt_min`）。主導項は平均 minus \((\log 2)/\beta\) で、\(-\infty\) へ発散します。**二値的なハード判定にはなりません。**

代数的な否定は \(\neg j=-j\) で、通常–双対スワップ \(\alpha\leftrightarrow\beta\) が誘導します。これは四つのラベルの上の関数ではありません。最深の \(B\)（\(J_{\mathrm{norm}}=-1\)）は \(F\) へ、内部の \(B\) は \(U\) へ行きます。\(\lambda\) 依存の否定は、この文書では具体形を与えていないので未定義のままです。

---

## 9. 通常時空によるバイアス（人間味の織り込み）

双対時空のみでは比較的対称的な多値構造が得られます。通常時空との相対差が加わることで、ポテンシャルに非対称項を導入できます：

\[
V_{\lambda,\gamma}(J) = -\cos(2\pi J) + \frac{\alpha}{\lambda} U(J) + \gamma\, J
\]

ここで \(\gamma\) は「好み・習慣・流行」に相当するバイアスパラメータです。

エネルギー最小化（勾配降下）のもとでは、線形項 \(\gamma J\) は \(\gamma J<0\) の側を下げます。したがって

- \(\gamma > 0\)：負トーション側（\(B\)）のエネルギーを下げ、正側を上げる
- \(\gamma < 0\)：正トーション側のエネルギーを下げる

です。好みの符号を正側に付けたいなら、バイアスは \(-\gamma J\) と書くか、最大化問題に切り替える必要があります。

\(J_{\mathrm{norm}}\) はスカラー値であるだけでなく、勾配流を通じて論理に速度を与え得ます。履歴依存そのものは、このポテンシャルだけではまだ形式化していません。

---

## 10. 振幅と幾何演算

命題の一次対象は、区間上のスカラーではなく許容ねじれ配置です。測定 \(\langle p\rangle=J_{\mathrm{norm}}(p)\) のあとで四つのラベルが現れます。新しい Clifford 代数は足していません。

| 層 | 担体 | Lean |
|----|------|------|
| 振幅 | 許容配置 \(p\)、\(\Omega(p)\)、\(R(p)=\exp\Omega(p)\) | `Amplitude` |
| 四状態 | \(J_{\mathrm{norm}}\in[-1,1]\) → `{T,U,F,B}` | `TruthValue`, `Connective` |
| 順序 | 高さ \(j\preceq_\tau k\iff j\le k\)、情報 \(j\preceq_\iota k\iff |j|\le|k|\) | `Order` |
| 幾何 | Killing 重なり、交換子、PGA 合成 | `Geometric` |

- **重なり** \(\langle p\mid q\rangle=\texttt{killingForm}\,p\,q\)。対称。自己対は \(16J\)（`overlap_self`）。Hilbert 内積ではない。
- **干渉** \([\Omega(p),\Omega(q)]\)。異軸では一般に 0 でない（`interfere_axis0_axis1_ne_zero`）。同軸では消える（`interfere_axis0_self`）。
- **合成** \(R(p)R(q)\) は PGA に留める。BCH で `TorsionParams` へは戻さない。
- **評価** `sandwich` を Gravity 非依存で再掲。退化計量の保存は主張しない。

情報順序では `T`（\(j=0\)）が唯一の底、頂は壁 \(\pm 1\)、すなわち `F` と最深 `B` です。内部の `B` は最深 `B` より情報的に真に小さいです。ラベル対応が違うので Belnap 同型は主張しません。

大規模極限では中間状態が豊かになり、真理値の格子構造が非分配的な特徴を帯びやすくなります。これは量子論理（オーソモジュラー格子）への連続的接近と**解釈**できますが、D4L のスカラー層では主張しません。実数上の \(\min/\max\) は分配的です。非分配性は兄弟である \(\mathbb{C}^2\) の部分空間格子（`Logic/Quantum/`）にあります。D4L の Killing 重なりは Hilbert 内積ではありません。

---

## 11. このアイデアの面白さ（再整理）

1. **古典論理を「小スケールでの有効理論」と読む道**はある。
   ただし書かれた \(U\) ではその収縮は起きない。二値は捨てる対象ではなく、別のポテンシャルのもとで現れる近似、として残すのが正直です。

2. **理想側（大規模）はすでに機械検証できる。**
   \(V_\infty\) の三点安定・盆地の切り分け・振幅と幾何演算までが、その核です。

3. **人間的なバイアスは符号を力学と揃える必要がある。**
   線形項 \(\gamma J\) は、最小化なら \(\gamma\) と逆符号の側を安定化します。

4. **計算との親和性**
   有限スケールの softmin は \(\min\) の滑らかな下界として実装できます。小 \(\beta\) を二値ハード判定だと思うのは誤りです。

---

## 12. まとめ

D4L は、次の連続的な族として提案されます：

\[
\bigl(V_{\lambda},\ \land_{\lambda},\ \lor_{\lambda},\ \neg_{\lambda}\bigr)_{\lambda > 0}
\]

いま機械検証できている核は次です。

- 命題の一次対象は許容配置（振幅）である
- 真理値の担い手は \(J_{\mathrm{norm}}\)（生の \(J\) ではない）
- \(\lambda\to\infty\)：\(V_\infty\) が \(\lvert B\rangle_{\mathrm{deep}}\)・\(\lvert T\rangle\)・\(\lvert F\rangle\) の三点を安定化し、\(\lvert U\rangle\) は通過領域
- 合接・選言・符号反転は実数上の \(\min/\max/-\) で、爆発しない
- Killing 重なりは対称、異軸のねじれ双ベクトルは一般に非可換
- 書かれた \(U\) のままでは \(\lambda\to 0\) の二値収縮は起きない

「論理は絶対的に一つではなく、系の大きさによって姿を変える」は、修正ポテンシャルを置いたあとの検証課題です。
入門のイメージは温度と水のままでよく、数式に進むときは §13 の辞書を先に読んでください。

---

## 13. Lean 検証で分かったこと

対象は `DstDiophantine/Logic/` です。数論三層（加法忠実化 / no-go / bridge）には依存させず、`lake build DstDiophantine.Logic` で回帰します。無条件の FLT / Beal / ゲーデル否定は主張しません。

幾何代数との接続は新しい代数を足すことではありません。すでに証明済みの PGA 双ベクトル `omegaTorsion` の Killing 二次形式が、正規化を経て四状態になる、という読みです。この文書のスケール \(\lambda\)、CGA の dilation、増幅の `scaleTorsion` は、いずれも「スケール」と呼ばれますが別物です。同一視はしていません。

### 13.1 辞書：区間 \([-1,1]\) は \(J_{\mathrm{norm}}\)

| 量 | 定義 | 許容錐上の範囲 | Lean |
|----|------|----------------|------|
| 生の \(J\) | \(\tfrac12\sum(\alpha^2-\beta^2)\) | \(\lvert J\rvert\le 3\pi^2/8\) | `J`, `torsion_bound_raw_continuous_pi_sq` |
| \(J_{\mathrm{norm}}\) | \(\frac{8}{3\pi^2}J\) | \(\lvert J_{\mathrm{norm}}\rvert\le 1\)、像はちょうど \([-1,1]\) | `JNormalized`, `torsion_bound_continuous`, `exists_admissible_JNormalized` |
| \(\frac1{16}B(\Omega,\Omega)\) | 生成子展開では \(\tfrac18\sum(\alpha^2-\beta^2)\) | \(J\) の \(1/4\) | `one_sixteenth_omegaKilling_eq`, `paper_appendix_killing_coeff_false` |

四状態の分類は `classifyOfMem` / `ofParams` です。許容配置では常に定義でき、四状態はすべて inhabited です（`exists_ofParams`）。\(\lvert F\rangle\) は純双曲、最深の \(\lvert B\rangle\)（\(J_{\mathrm{norm}}=-1\)）は純楕円です。

### 13.2 書かれた \(U\) では \(B\) 井戸は消えない

区分的 \(U\) は負領域で恒等的に 0 です。よってすべての \(\lambda>0\) で

\[
V_\lambda(J)=V_\infty(J)\qquad(J<0),\qquad
V_\lambda''(-1)=4\pi^2>0.
\]

負トーションの局所最小はスケール非依存に残ります（`written_U_B_well_survives`, `written_U_no_critical_scale`）。小スケールで \(\lvert B\rangle\) が消えて \(\{T,F\}\) に収縮する、は**この \(U\) の定理ではありません**。

二つの \(U\) は正領域で定数倍だけ違います。定量的な臨界値を論じる前に、どちらを公式とするかを決める必要があります。

### 13.3 大規模ダイナミクス

\(V_\infty=-\cos(2\pi J)\) では、安定点が三つの端点、盆地が §6 の表です。\(\lvert U\rangle\) はラベルであってアトラクタではありません。

### 13.4 結合子：爆発しない。否定はラベルの関数ではない

実数上で \(\neg j=-j\)、\(\wedge=\min\)、\(\vee=\max\) と置きます。De Morgan と分配則は普通の実数のものです。

\[
j\wedge\neg j=\min(j,-j)=-|j|\le 0.
\]

したがって \(p\wedge\neg p\) は \(T\)（\(j=0\)）または \(B\) であり、決して \(F\) になりません（`classify_conj_neg_ne_F`）。

通常–双対スワップ `daggerParams` が \(J_{\mathrm{norm}}\mapsto -J_{\mathrm{norm}}\) を誘導するので、符号反転は新しい論理演算ではなく PGA の対合です。しかし四つの**名前**の上では関数になりません（`neg_not_a_function_of_TruthValue`）。

| \(J_{\mathrm{norm}}\) | 状態 | \(-J_{\mathrm{norm}}\) | 像の状態 |
|----------------------|------|------------------------|----------|
| \(0\) | \(T\) | \(0\) | \(T\) |
| \(1\) | \(F\) | \(-1\) | 最深 \(B\) |
| \(-1\) | 最深 \(B\) | \(1\) | \(F\) |
| \((-1,0)\) | \(B\) | \((0,1)\) | \(U\) |
| \((0,1)\) | \(U\) | \((-1,0)\) | \(B\) |

softmin は \(\beta\to\infty\) で \(\min\) に収束し、有限の \(\beta>0\) では常に \(\min\) より小さいです。小 \(\beta\) を二値ハード判定と読むのは誤りです。

### 13.5 バイアスの符号

\(V_{\lambda,\gamma}=V_\lambda+\gamma J\) を最小化すると、\(\gamma J<0\) の側が得をします。\(\gamma>0\) は負トーションを相対的に安定化します。正側を好む gloss を残すなら、項を \(-\gamma J\) に直すのが一貫します。

### 13.6 いま主張しないこと

- 書かれた \(U\) による四値→二値の相転移
- \(\lambda\) と CGA dilation / `scaleTorsion` の同一視
- 量子論理（オーソモジュラー格子）への連続的接近
- ゲーデル文が古典算術で偽であること
- 無条件の FLT / Beal / abc
- D4L としての Hilbert 空間、Born 則（Killing 自己対は負になり得る）
- \(R(p)R(q)\) が常にねじれローターへ戻ること

双対セクターの兄弟構成（\(\mathbb{C}^2\)、四元数表、部分空間格子、四ラベルは PVM でない）は `DstDiophantine/Logic/Quantum/` と論文 `dst-d4l-hilbert.tex` にある。D4L との同一視ではない。

相転移を残したい場合の次の仕事は、負領域で凹な修正 \(U\)、または周期項の前に \(\lambda\) が乗る Landau 型を、別モジュールに置くことです。

論文は `dst-4-valued-logic.tex`（日本語 `dst-4-valued-logic-ja.tex`）です。Hilbert 兄弟層は `dst-d4l-hilbert.tex`。

---

*この文書は Dual Spacetime Theory（二重時空理論）と D4L の議論に基づき、`DstDiophantine.Logic` の機械検証で改訂しています。*
